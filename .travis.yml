language: python
python:
  - "3.6"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: "pytest --cov-report term --cov=ajson_rpc2; python accept_tests/accept_test.py"
